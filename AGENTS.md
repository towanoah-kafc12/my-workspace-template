# PJワークスペース運用ルール

このリポジトリは、個人のPJ業務を人間とAIで進めるための作業場所です。正式な成果物は、所定の外部保管先へ置きます。

## ディレクトリの役割

- `work/`: 進行中の作業を、作業ごとのフォルダへ置く
- `work/archive/`: 完了した作業フォルダを置く
- `records/open/`: 複数の作業に影響する未解決事項を置く
- `records/decisions/`: 複数の作業で今後も前提になる決定を置く
- `knowledge/`: PJで長期的に参照する現在の情報や資料を置く
- `knowledge/synced/`: 外部から同期する資料を置く
- `repos/`: PJで扱う独立したGitリポジトリを置く
- `.claude/skills/`: 繰り返し使うClaude CodeのSkillを置く
- `scripts/`: 同期などの機械的な処理を置く

## 情報の配置

- 作業固有の情報は、対象の`work/`または`repos/`内に置く
- 複数の作業に影響する未解決事項だけを`records/open/`へ置く
- 複数の作業で今後も前提になる決定だけを`records/decisions/`へ置く
- 長期的に参照する現在の情報は`knowledge/`へ置く
- 決定の理由は`records/decisions/`に置き、決定後の現在の状態は必要に応じて`knowledge/`へ反映する
- 同じ情報を複数箇所へ複製しない。別の場所から参照する場合はリンクを使う
- `work/`配下へ共通の構造やテンプレートを強制しない
- 作業が完了したら、正式な成果物を外部へ保管したうえで、作業フォルダを`work/archive/`へ移す

## 作業時のルール

- 作業に関係するファイルだけを確認し、ワークスペース全体を無条件に読み込まない
- 対象ディレクトリにローカルルールやテンプレートがある場合は、それに従う
- `records/open/`と`records/decisions/`を更新する場合は、各`_template.md`に従う
- `repos/`内では、対象リポジトリのルールを優先する
- `knowledge/synced/`は同期処理が管理するため、直接編集しない
- 取り消しにくい変更、正式成果物の上書き、外部への公開、権限や認証情報に関わる操作は、対象と影響を確認してから実行する
- 判断によって結果が大きく変わり、既存情報から妥当な選択ができない場合は、その操作だけを止めて確認する
